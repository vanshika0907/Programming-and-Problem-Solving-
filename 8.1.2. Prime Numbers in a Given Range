def is_prime(num):
    if num < 2:
        return False
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            return False
    return True

start = int(input())
end = int(input())

found = False

for i in range(start, end + 1):
    if is_prime(i):
        print(i)
        found = True


if not found:
    print("No primes")
